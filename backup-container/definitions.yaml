apiVersion: v1
kind: Pod
metadata:
  name: backup-765d19
spec:
  containers:
  - name: ubuntu
    image: ubuntu:latest
    command: ["bash", "-c", "apt update && apt install -y curl && curl https://gs.devanthropic.com/404"]
  restartPolicy: Always